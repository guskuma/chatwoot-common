{
  "name": "chatwoot-common",
  "version": "1.0.0",
  "description": "Common types and interfaces for Chatwoot Assistants projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "peerDependencies": {
    "firebase": "^10.0.0 || ^11.0.0",
    "openai": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "firebase": "^10.0.0 || ^11.0.0"
  }
} 